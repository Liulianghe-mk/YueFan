# 本地联调：数据写入文件，重启后端后账号仍保留（mvn spring-boot:run -Dspring-boot.run.profiles=local）
spring:
  datasource:
    url: jdbc:h2:file:./.data/yuefan;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE;MODE=MySQL;DATABASE_TO_LOWER=TRUE
